import _thread
from wallsches import wallsches as wallsches

_thread.start_new_thread(wallsches, (10**6,))
_thread.start_new_thread(wallsches, (10**7,))
_thread.start_new_thread(wallsches, (10**8,))
_thread.start_new_thread(wallsches, (10**9,))
_thread.start_new_thread(wallsches, (10**10,))
_thread.start_new_thread(wallsches, (10**11,))
_thread.start_new_thread(wallsches, (10**12,))
_thread.start_new_thread(wallsches, (10**13,))
_thread.start_new_thread(wallsches, (10**14,))
_thread.start_new_thread(wallsches, (10**15,))
_thread.start_new_thread(wallsches, (10**16,))

        
while True:
    pass
